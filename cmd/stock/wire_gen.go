// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v3"
	"github.com/marketing-platform/internal/conf"
	"github.com/marketing-platform/internal/stock/biz"
	"github.com/marketing-platform/internal/stock/data"
	"github.com/marketing-platform/internal/stock/server"
	"github.com/marketing-platform/internal/stock/service"
	"log/slog"
)

// Injectors from wire.go:

func wireApp(confServer *conf.Server, confData *conf.Data, logger *slog.Logger) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData)
	if err != nil {
		return nil, nil, err
	}
	stockRepo := data.NewStockRepo(dataData)
	stockService := biz.NewStockService(stockRepo)
	serviceStockService := service.NewStockService(stockService)
	httpServer := server.NewHTTPServer(confServer, serviceStockService)
	app := newApp(logger, httpServer)
	return app, func() {
		cleanup()
	}, nil
}
