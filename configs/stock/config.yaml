server:
  http:
    addr: 0.0.0.0:18094
    timeout: 10s

data:
  database:
    driver: mysql
    source: ${MYSQL_DSN:root:root@tcp(127.0.0.1:3306)/marketing_stock?charset=utf8mb4&parseTime=True&loc=Local}
    debug: true
    auto_migrate: true
