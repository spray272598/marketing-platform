// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/marketing-platform/internal/seckill/data/ent/seckillactivity"
)

// SeckillActivity is the model entity for the SeckillActivity schema.
type SeckillActivity struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt *time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt *time.Time `json:"updated_at,omitempty"`
	// ActivityID holds the value of the "activity_id" field.
	ActivityID string `json:"activity_id,omitempty"`
	// ActivityName holds the value of the "activity_name" field.
	ActivityName string `json:"activity_name,omitempty"`
	// SkuID holds the value of the "sku_id" field.
	SkuID string `json:"sku_id,omitempty"`
	// TotalCount holds the value of the "total_count" field.
	TotalCount int32 `json:"total_count,omitempty"`
	// LimitCount holds the value of the "limit_count" field.
	LimitCount int32 `json:"limit_count,omitempty"`
	// ActivityState holds the value of the "activity_state" field.
	ActivityState int32 `json:"activity_state,omitempty"`
	// StartTime holds the value of the "start_time" field.
	StartTime time.Time `json:"start_time,omitempty"`
	// EndTime holds the value of the "end_time" field.
	EndTime      time.Time `json:"end_time,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SeckillActivity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case seckillactivity.FieldTotalCount, seckillactivity.FieldLimitCount, seckillactivity.FieldActivityState:
			values[i] = new(sql.NullInt64)
		case seckillactivity.FieldActivityID, seckillactivity.FieldActivityName, seckillactivity.FieldSkuID:
			values[i] = new(sql.NullString)
		case seckillactivity.FieldCreatedAt, seckillactivity.FieldUpdatedAt, seckillactivity.FieldStartTime, seckillactivity.FieldEndTime:
			values[i] = new(sql.NullTime)
		case seckillactivity.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SeckillActivity fields.
func (_m *SeckillActivity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case seckillactivity.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case seckillactivity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = new(time.Time)
				*_m.CreatedAt = value.Time
			}
		case seckillactivity.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = new(time.Time)
				*_m.UpdatedAt = value.Time
			}
		case seckillactivity.FieldActivityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field activity_id", values[i])
			} else if value.Valid {
				_m.ActivityID = value.String
			}
		case seckillactivity.FieldActivityName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field activity_name", values[i])
			} else if value.Valid {
				_m.ActivityName = value.String
			}
		case seckillactivity.FieldSkuID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sku_id", values[i])
			} else if value.Valid {
				_m.SkuID = value.String
			}
		case seckillactivity.FieldTotalCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_count", values[i])
			} else if value.Valid {
				_m.TotalCount = int32(value.Int64)
			}
		case seckillactivity.FieldLimitCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field limit_count", values[i])
			} else if value.Valid {
				_m.LimitCount = int32(value.Int64)
			}
		case seckillactivity.FieldActivityState:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field activity_state", values[i])
			} else if value.Valid {
				_m.ActivityState = int32(value.Int64)
			}
		case seckillactivity.FieldStartTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_time", values[i])
			} else if value.Valid {
				_m.StartTime = value.Time
			}
		case seckillactivity.FieldEndTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_time", values[i])
			} else if value.Valid {
				_m.EndTime = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SeckillActivity.
// This includes values selected through modifiers, order, etc.
func (_m *SeckillActivity) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this SeckillActivity.
// Note that you need to call SeckillActivity.Unwrap() before calling this method if this SeckillActivity
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SeckillActivity) Update() *SeckillActivityUpdateOne {
	return NewSeckillActivityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SeckillActivity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SeckillActivity) Unwrap() *SeckillActivity {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SeckillActivity is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SeckillActivity) String() string {
	var builder strings.Builder
	builder.WriteString("SeckillActivity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.CreatedAt; v != nil {
		builder.WriteString("created_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.UpdatedAt; v != nil {
		builder.WriteString("updated_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("activity_id=")
	builder.WriteString(_m.ActivityID)
	builder.WriteString(", ")
	builder.WriteString("activity_name=")
	builder.WriteString(_m.ActivityName)
	builder.WriteString(", ")
	builder.WriteString("sku_id=")
	builder.WriteString(_m.SkuID)
	builder.WriteString(", ")
	builder.WriteString("total_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalCount))
	builder.WriteString(", ")
	builder.WriteString("limit_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.LimitCount))
	builder.WriteString(", ")
	builder.WriteString("activity_state=")
	builder.WriteString(fmt.Sprintf("%v", _m.ActivityState))
	builder.WriteString(", ")
	builder.WriteString("start_time=")
	builder.WriteString(_m.StartTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_time=")
	builder.WriteString(_m.EndTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SeckillActivities is a parsable slice of SeckillActivity.
type SeckillActivities []*SeckillActivity
